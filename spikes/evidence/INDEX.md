# Spike evidence index

Environment/version JSON, test output logs and run summaries for Spike 01–04 are stored in this directory. Spike-specific visual frames remain under `spikes/spike-02-video-render/evidence/frames/`.
