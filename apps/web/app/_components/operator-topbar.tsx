export function OperatorTopbar({ title = '运营控制台', projectName }: { title?: string; projectName?: string }) { return <header className="operator-topbar"><div><p className="eyebrow">ContentOS / 运营台</p><h1>{title}</h1></div>{projectName && <span className="topbar-context">项目：{projectName}</span>}</header>; }
