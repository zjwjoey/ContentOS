import { StatusBadge } from '../../app/_components/status-badge';
export type VideoAsset = { id: string; kind?: string; lifecycle?: string; originalName: string; metadata?: { durationMs?: number; width?: number; height?: number }; contentUrl?: string };
export function AssetCard({ asset, selected, onSelect }: { asset: VideoAsset; selected?: boolean; onSelect?: () => void }) { return <button type="button" className={`asset-card${selected ? ' selected' : ''}`} onClick={onSelect}><strong>{asset.originalName}</strong><span className="asset-meta">{asset.kind || 'MEDIA'} · {asset.metadata?.durationMs ? `${Math.round(asset.metadata.durationMs / 1000)}s` : '时长待定'} {asset.metadata?.width ? `· ${asset.metadata.width}×${asset.metadata.height}` : ''}</span>{asset.lifecycle && <StatusBadge status={asset.lifecycle} />}</button>; }
