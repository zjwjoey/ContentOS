export function ManifestRevisionPicker({ revisions, currentId, onSelect }: { revisions: Array<{ id: string; revision: number; status?: string }>; currentId?: string; onSelect: (id: string) => void }) { return <label>Manifest Revision<select value={currentId || ''} onChange={(event) => onSelect(event.target.value)}><option value="" disabled>选择版本</option>{revisions.map((item) => <option key={item.id} value={item.id}>v{item.revision} · {item.status === 'SUPERSEDED' ? '已替代' : '当前'}</option>)}</select></label>; }
