type Material = { id: string; originalName: string; durationMs: number; thumbnailUrl?: string };
export function MediaFilterBar({ query, orientation, usage, onQuery, onOrientation, onUsage, assets = [], selected = [], onToggle }: { query: string; orientation: string; usage: string; onQuery: (value: string) => void; onOrientation: (value: string) => void; onUsage: (value: string) => void; assets?: Material[]; selected?: string[]; onToggle?: (id: string) => void }) {
  return <><div className="material-filters"><input aria-label="搜索素材" placeholder="搜索素材或标签" value={query} onChange={(event) => onQuery(event.target.value)} /><select aria-label="素材方向" value={orientation} onChange={(event) => onOrientation(event.target.value)}><option value="ALL">全部方向</option><option value="VERTICAL">竖屏</option><option value="HORIZONTAL">横屏</option><option value="SQUARE">方形</option><option value="UNKNOWN">未知</option></select><select aria-label="素材使用情况" value={usage} onChange={(event) => onUsage(event.target.value)}><option value="ALL">全部使用情况</option><option value="UNUSED">未使用</option><option value="FREQUENT">高频使用</option></select></div>{assets.length > 0 && <div className="material-thumb-grid">{assets.map((asset) => <button type="button" key={asset.id} className={selected.includes(asset.id) ? 'selected' : ''} onClick={() => onToggle?.(asset.id)}>{asset.thumbnailUrl ? <img src={asset.thumbnailUrl} alt="" loading="lazy" /> : <span>素材</span>}<strong>{asset.originalName}</strong><small>{(asset.durationMs / 1000).toFixed(1)} 秒</small></button>)}</div>}</>;
}
