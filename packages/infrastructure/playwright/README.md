# Playwright infrastructure

Reserved for a later authorized Publisher Adapter integration.
Playwright is an infrastructure implementation of the Publisher browser-session contract.

It is composed only by the Publisher Worker. API handlers and module services must not import Playwright or launch a browser.
